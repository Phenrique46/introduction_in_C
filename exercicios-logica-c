#include <stdio.h>

int main(){
int a, b, c;

  printf("Digite três numeros:");
  scanf("%d %d %d", &a, &b, &c);
  printf("Soma: %d\n", a + b + c);
  printf("Subtracao: %d\n", a - b - c);
  printf(Multiplicacao: %d\n", a * b * c);
  printf(Divisao: %d\n", a / b / c);


return 0;

}
